from controllers import index, results, add_proizvod, view_add_proizovd

routes = {
    '/': index,
    '/results': results,
    '/add-proizvod': add_proizvod,
    'view-add-proizvod': view_add_proizovd
}